must use bash in vsc or in or use git
npm install -g pkg -- to run pkg install

all types of installation

echo "Cleaning old builds..."
rm -rf dist
mkdir dist

echo "Building Windows exe..."
pkg . --targets node18-win-x64 --output dist/app-win.exe

echo "Done! Files are in /dist"

exec type
chmod +x build.sh
then run
./build.sh
